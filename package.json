{
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "db:migrate": "tsx src/db/migrate.ts",
    "dead-letters:replay": "tsx src/admin/replay-dead-letters.ts",
    "test:dead-letters": "node --import tsx --test test/dead-letter-replay.test.ts"
  }
}
