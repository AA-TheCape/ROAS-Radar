- `roas-radar-meta-ads-sync*` must deploy with `META_ADS_JOB_MODE=spend` and only the spend worker entrypoint `npm run meta-ads:sync:start`.
- `roas-radar-meta-order-value-sync*` must deploy with `META_ADS_JOB_MODE=order_value` and only the order-value entrypoint `npm run meta-ads:order-value:start`.
- Spend job logs and queue metrics are labeled with `pipeline=meta_ads_spend`; order-value lifecycle and sync logs are labeled with `pipeline=meta_ads_order_value`.
