Updated:
- topology counts
- added GA4 ingestion job and scheduler to the managed workload list
- added required env vars for GA4 ingestion
- updated the default scheduled sequence to include hourly GA4 ingestion
- updated staging verification steps to execute and verify the GA4 job
