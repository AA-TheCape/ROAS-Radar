{
	"name": "roas_order_attribution_confidence_backfill_progress",
	"description": "Tracks confidence metadata backfill progress, fallback lookup rows, and skipped-vs-recomputed dimensions.",
	"filter": "resource.type=\"cloud_run_revision\" AND jsonPayload.event=\"order_attribution_confidence_backfill_progress\"",
	"labelExtractors": {
		"service": "EXTRACT(jsonPayload.service)",
		"stage": "EXTRACT(jsonPayload.stage)",
		"dry_run": "EXTRACT(jsonPayload.dryRun)"
	},
	"metricDescriptor": {
		"metricKind": "DELTA",
		"valueType": "INT64",
		"unit": "1",
		"displayName": "ROAS Radar Order Attribution Confidence Backfill Progress",
		"labels": [
			{
				"key": "service",
				"valueType": "STRING",
				"description": "Service emitting the progress event."
			},
			{
				"key": "stage",
				"valueType": "STRING",
				"description": "Backfill progress stage."
			},
			{
				"key": "dry_run",
				"valueType": "STRING",
				"description": "Whether the backfill ran without writes."
			}
		]
	}
}
