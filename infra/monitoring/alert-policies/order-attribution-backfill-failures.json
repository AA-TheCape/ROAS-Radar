{
	"displayName": "ROAS Radar __ENVIRONMENT__ Order Attribution Backfill Failures",
	"documentation": {
		"content": "Order attribution backfill jobs are failing in Cloud Run. Inspect `order_attribution_backfill_job_lifecycle` logs by `jsonPayload.jobId` and review the attached failure metadata. Runbook: __RUNBOOK_URL_ATTRIBUTION__",
		"mimeType": "text/markdown"
	},
	"combiner": "OR",
	"enabled": true,
	"severity": "WARNING",
	"notificationChannels": "__ALERT_NOTIFICATION_CHANNELS__",
	"conditions": [
		{
			"displayName": "Order attribution backfill failures exceed 0 in 15 minutes",
			"conditionThreshold": {
				"filter": "metric.type=\"logging.googleapis.com/user/roas_order_attribution_backfill_jobs\" resource.type=\"cloud_run_revision\" metric.labels.stage=\"failed\"",
				"comparison": "COMPARISON_GT",
				"thresholdValue": 0,
				"duration": "900s",
				"aggregations": [
					{
						"alignmentPeriod": "900s",
						"perSeriesAligner": "ALIGN_SUM"
					}
				],
				"trigger": {
					"count": 1
				}
			}
		}
	]
}
